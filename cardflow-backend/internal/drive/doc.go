// Package drive implements Google Drive OAuth connectivity and file operations.
//
// The package follows the repo's feature-first layout: it exposes a repository
// port, a service for business logic, and an Echo HTTP handler for routes.
package drive

{
  "name": "@cardflow-app/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/api",
    "build": "go build -o bin/api.exe ./cmd/api",
    "test": "go test ./...",
    "db:up": "docker compose up -d postgres redis",
    "db:down": "docker compose down"
  }
}
